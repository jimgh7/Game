language:                               

c:                                      
  - #language version


services:                                       
  #any supported service                      

env:                                                
  - #env1=foo                           
  - #env2=bar

matrix:                                                                 

build:       

  pre_ci:                               

  pre_ci_boot:                          
    image_name:
    image_tag:
    pull:
    options:
  ci:
    g++ Src/main.cpp
  post_ci:
  on_success:
    echo "Success"
  on_failure:
  cache:
  cache_dir_list:
  push:


